# Historical workflows

These workflow files are retained as development evidence but are stored
outside `.github/workflows/`, so GitHub Actions does not execute them.

`cover-art-baseline.yml` records the original inherited SuperFW baseline build
used before the SuperR7 interface work. Active tests and release-candidate
workflows live under `.github/workflows/`.
